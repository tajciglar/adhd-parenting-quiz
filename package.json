{
  "name": "adhd-parenting-quiz",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "build:shared": "pnpm --filter @adhd-parenting-quiz/shared build",
    "dev:api": "pnpm run build:shared && pnpm --filter @adhd-parenting-quiz/api dev",
    "build:api": "pnpm run build:shared && pnpm --filter @adhd-parenting-quiz/api build",
    "dev:web": "pnpm run build:shared && pnpm --filter web dev",
    "build:web": "pnpm run build:shared && pnpm --filter web build",
    "dev:checkout": "pnpm --filter @adhd-parenting-quiz/checkout dev",
    "build:checkout": "pnpm --filter @adhd-parenting-quiz/checkout build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.29.2",
  "pnpm": {
    "onlyBuiltDependencies": [
      "prisma",
      "@prisma/client",
      "@prisma/engines",
      "esbuild"
    ]
  }
}
