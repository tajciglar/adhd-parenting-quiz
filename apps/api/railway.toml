[build]
builder = "nixpacks"
buildCommand = "pnpm --filter @adhd-parenting-quiz/shared build && pnpm --filter @adhd-parenting-quiz/api build"

[deploy]
startCommand = "node apps/api/dist/server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
